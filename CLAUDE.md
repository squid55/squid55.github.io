# memory bank — 블로그 저장소

Hugo 로 만들고 GitHub Pages 로 배포하는 개인 블로그. <https://squid55.github.io/>
저장소 `squid55/squid55.github.io` 는 **공개**다. 여기 커밋하는 것은 전부 인터넷에 공개된다.

## 명령

- `/post` — 글 하나를 새로 올린다. 분류 결정 → 파일 생성 → 사진·PDF 처리 → 빌드 검사 → 확인 → 커밋·푸시.
  자세한 절차는 `.claude/skills/post/SKILL.md` 에 있다.

## 구조

```
content/
├── kernel/{v4l2,ebpf,internals}/   리눅스 커널
├── embedded/                       임베디드 펌웨어 · C/C++ 구조
├── conference/<슬러그>/            컨퍼런스·모임 후기 (폴더 하나가 글 하나, 사진을 같이 둔다)
├── guides/<슬러그>/                PDF 가이드 (PDF 를 같이 둔다)
└── about.md
```

- 새 글은 `hugo new content <경로>` 로 만든다. 틀은 `archetypes/` 에 있다.
- 사이트에 분류 메뉴가 없다. 대문은 날짜순 목록 하나다. 분류는 글 위의 경로로만 드러난다.

## front matter

| 키 | 쓰는 곳 | 뜻 |
|---|---|---|
| `title` | 전부 | 제목. 사이트 이름을 반복하지 않는다 |
| `description` | 전부 | 목록에 한 줄로 붙는 부제 |
| `date` | 전부 | **글을 쓴 날.** 행사 날짜가 아니다 |
| `lastmod` | 과거 자료를 뒤늦게 올릴 때 | 없으면 git 커밋 날짜가 수정일로 잡힌다 |
| `tags` | 전부 | 소문자·있는 그대로. 자동 대문자화는 꺼 두었다 |
| `cover` | 사진이 있는 글 | 목록 썸네일 · 링크 공유 이미지 |
| `pdf` | 가이드 | 본문 아래 PDF 뷰어 + 내려받기 |
| `kernel` / `target` | 커널 글 / 임베디드 글 | 분석한 커널 버전 / 대상 보드 |
| `series` | 연재 | 같은 이름끼리 묶어 글 머리에 목록을 보여준다 |
| `toc` | 긴 글 | 오른쪽 목차 |
| `draft: true` | 미완성 | 배포되지 않는다. 올릴 때는 지운다 |

## 본문에서 쓰는 것

````markdown
[다른 글](../ebpf/verifier.md)          파일 경로로 링크. 깨지면 빌드 실패
![설명](img/a.jpg "캡션")               번들 안 사진. 1400px 로 줄여 내보내고 누르면 확대
{{< gallery match="img/day1-*" >}}      사진 격자
{{< note measured >}}…{{< /note >}}     실측 · warn · info · todo
{{< pdf src="extra.pdf" >}}             본문 중간에 PDF
{{< svg src="pipeline.svg" >}}          SVG 를 본문에 직접 심어 다크 모드를 따르게
```mermaid                              시퀀스 · 흐름도
````

## 문체

이 블로그의 글은 **사용자가 직접 쓴 것처럼 읽혀야 한다.** AI 가 쓴 티가 나면 안 된다.

- 평서문. 「~이다」 「~한다」. 존댓말을 쓰지 않는다.
- 사용자가 말했거나 자료에 있는 내용만 쓴다. 감상·판단을 지어내지 않는다.
- 모르는 것은 「잘 모른다」 「확인하지 못했다」로 남긴다. 아는 척하지 않는다.
- 직접 확인한 것만 「실측」으로 표시한다.
- 문단마다 볼드를 넣지 않는다. 이모지를 쓰지 않는다.
- 「~에 대해 알아보겠습니다」 「결론적으로」 같은 상투구, 과장된 마무리를 쓰지 않는다.
- 분류 설명문, 「작성 예정」 같은 빈자리 문구를 새로 만들지 않는다.

## 사진

- **커밋 전에 반드시** `python3 tools/prep_photos.py <원본...> -o <글폴더>/img` 를 돌린다.
  긴 변 2500px 로 줄이고 EXIF(GPS 포함)를 지운다.
- 방향이 틀어진 사진이 섞일 수 있다. EXIF 방향값만 믿지 말고 눈으로 확인한다.
  누워 있는 것은 돌리고, 값만 잘못된 것은 값만 지운다.
- 사이트로 나가는 사진은 Hugo 가 다시 인코딩해 EXIF 가 빠지지만, 저장소의 원본에는 남는다.

## 확인과 배포

```bash
hugo server -D --enableGitInfo=false      # 로컬 미리보기
hugo --gc --minify --panicOnWarning       # 커밋 전 검사. 깨진 링크·없는 사진이 있으면 실패
```

`main` 에 푸시하면 `.github/workflows/pages.yml` 이 빌드해 배포한다. 1~2분 걸린다.
Pages 설정의 build type 은 **workflow** 여야 한다. legacy 로 돌아가면 Jekyll 이 README 만 올린다.

## 하지 않는 것

- 발표 자료·가이드의 공개 여부를 다시 묻지 않는다. 공유된 자료라는 판단을 받았다. 출처·발표자 표기는 계속 붙인다.
- 사용자가 요청하지 않은 글·분류·문구를 만들지 않는다.
- 사용자 개인정보(사진의 GPS 등)는 묻지 말고 미리 지운다.
